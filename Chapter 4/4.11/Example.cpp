#include "stdafx.h"
#include <iostream>
using namespace std;

int main()
{
	if (age >= 65)
		cout << "Age is greater than or equal to 65" << endl;
	else
		cout << "Age is less than 65" << endl;



	int x = 1;
	int total=0;
	while (x <= 10)
	{
		total += x;
		++x;
	}

	while (x <= 100)
	{
		total += x;
		++x;
	}

	while (y > 0)
	{
		++y;
		cout << y << endl;
	}

}
